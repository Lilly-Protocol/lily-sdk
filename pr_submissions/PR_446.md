Fixes #446

- Validate per-request `timeoutMs` in fetch transport before initiating fetch request
- Reject non-numeric, negative, or non-finite timeout options with `LilyConfigError`
- Allow `timeoutMs: 0` as documented opt-out to disable request timeout
- Ensure request-level timeout overrides properly propagate to AbortController signal
- Add unit tests verifying timeout bounds, validations, and abort signaling

Test Output:
Test Files  1 passed (1)
Tests       12 passed (12)
Duration    375ms
